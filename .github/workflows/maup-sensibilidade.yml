name: Testes da aplicação A5

on:
  push:
    paths:
      - "applications/maup_sensibilidade/**"
      - ".github/workflows/maup-sensibilidade.yml"
  pull_request:
    paths:
      - "applications/maup_sensibilidade/**"
      - ".github/workflows/maup-sensibilidade.yml"

jobs:
  tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: applications/maup_sensibilidade
    env:
      MPLBACKEND: Agg

    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: applications/maup_sensibilidade/requirements.txt

      - name: Preparar ferramentas de construção
        run: python -m pip install --upgrade pip setuptools wheel

      - name: Instalar dependências
        run: python -m pip install -e ".[dev]"

      - name: Executar testes
        run: python -m pytest -q

      - name: Executar demonstração
        run: |
          python scripts/create_demo_data.py --output-dir data/demo
          maup-sensibilidade config/demo.yml
